{
  "evidence_id": "LP-T099-NETWORK-REQUIREMENTS-20260921",
  "scope": "Local immutable-source discovery and review-only catalog draft; no live cluster mutation",
  "requirement": "Fixed ingress hosts and externally exposed Services require human and target-cluster review before catalog activation",
  "red": [
    {
      "test": "test_network_exposure_requires_target_review_without_copying_hostnames",
      "result": "failed: network_exposure inventory absent"
    },
    {
      "test": "test_alternate_external_network_requirements_are_not_silently_supported",
      "result": "failed: malformed network spec was silently omitted"
    }
  ],
  "green": {
    "focused_command": "PYTHONPATH=backend:. .venv/bin/pytest -q backend/tests/test_catalog_intake_quality.py backend/tests/test_catalog_onboarding.py --tb=short --disable-warnings",
    "result": "34 passed",
    "regression_command": "PYTHONPATH=backend:. .venv/bin/pytest -q backend/tests -m 'not local' --disable-warnings --tb=short",
    "regression_result": "2100 passed, 27 skipped, 13 deselected"
  },
  "coverage": [
    "Route fixed host or subdomain",
    "Ingress rule or TLS host",
    "Service LoadBalancer, NodePort, ExternalName, or externalIPs",
    "Malformed network spec",
    "Ordinary ClusterIP and generated-host Route remain unflagged",
    "Finding contains only a stable code and evidence count, not hostnames",
    "Generated draft remains internal-only, one-seat, and non-orderable"
  ],
  "limitations": [
    "Static discovery cannot establish that a destination network supports the requirement",
    "Rendered templates, target evidence, participant journey, and live certification remain open"
  ]
}
