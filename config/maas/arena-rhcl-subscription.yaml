# Arena MaaS prerequisite. Manual InstallPlan approval is intentional: inspect
# dependencies and cluster health before installing, without touching live routes.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhcl-operator
  namespace: openshift-operators
  labels:
    app.kubernetes.io/part-of: launchpad-maas-bootstrap
spec:
  channel: stable
  installPlanApproval: Manual
  name: rhcl-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: rhcl-operator.v1.4.3
