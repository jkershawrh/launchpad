apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: launchpad-flightpath-candidate

resources:
  - ../flightpath-candidate
  - migration-job.yaml

patches:
  - path: hold-workloads.yaml
  - path: suspend-migration.yaml
