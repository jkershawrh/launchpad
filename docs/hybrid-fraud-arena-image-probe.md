# Hybrid Fraud GHCR candidate: Arena pull probe

The current draft candidate is `ghcr.io/jkershawrh/hybrid-fraud-detection@sha256:80ddeab0ffab077c64f3dbce7e90482fc02f3d32ea45cb755279f7dcdb5b10c0` from source commit `00f07a8265579efd77964b390d65368999a1523a`. [GitHub Actions run 35741711285](https://github.com/jkershawrh/hybrid-fraud-detection/actions/runs/35741711285) passed its tests, SBOM, signing, attestation, and verification steps. Arena pulled and smoke-tested this exact digest in a disposable namespace; see the [current pull receipt](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-pull-probe-secret-runtime-20260922.json). The namespace was deleted and its absence was verified. The earlier [pull receipt](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-pull-probe-passed-20260922.json) applies only to the superseded source and image.

Use the repository probe in dry-run mode first:

```bash
.venv/bin/python scripts/certify_arena_candidate_pull.py \
  --context 'default/api-arena-fm2aihpcsed-com:6443/kube:admin' \
  --image 'ghcr.io/jkershawrh/hybrid-fraud-detection@sha256:80ddeab0ffab077c64f3dbce7e90482fc02f3d32ea45cb755279f7dcdb5b10c0'
```

After Arena authentication is restored, add `--execute`. The probe verifies the exact Arena API server and authenticated identity before mutation. It creates a unique, labeled namespace, runs one non-privileged Pod with `imagePullPolicy: Always`, checks that the Pod completes the readiness/scoring smoke call using the exact digest, then deletes only the namespace UID it created. It never changes the active CLI context. Output is a sanitized status receipt; cluster stderr and application logs are not copied into it. If cleanup cannot be verified, the receipt is blocked and names the namespace for manual investigation.

This is a **single candidate pull probe**, not proof of cold-cache behavior on every eligible worker, production registry admission, trusted isolated rendering, model integration, Showroom usability, or release eligibility. The current platform artifact-registry policy still selects Quay for platform images. It must not be broadly changed to accept GHCR for unrelated catalog items; any future catalog-image exception needs explicit, scoped policy and destination evidence. The draft intake remains non-orderable until those gates and the measured seat lifecycle pass.

On September 22, 2026, an initial execution attempt returned `arena-auth-unavailable` during preflight. No namespace or Pod was created. After the isolated Arena CLI credential was refreshed, the probe passed and cleaned up its namespace. The active global CLI context did not change.

A separate [one-seat chart canary](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-one-seat-chart-canary-20260922.json) verified the two-container deployment, UI route, simulator path, and one live Granite 8B scoring call for the **earlier** image. The live model initially timed out because the disposable namespace lacked the `app.kubernetes.io/managed-by=launchpad` label required by Arena's model ingress policy; it became ready after that label was applied. This is an important namespace contract for any future Launchpad order. The chart requests 350m CPU and 512Mi memory for one Pod, but a single observed usage snapshot is **not** a complete seat-capacity measurement. The revised Secret-backed chart has only passed local tests and offline rendering, not a live full-seat journey. Showroom, identity/RBAC, trusted TLS, per-seat model authorization, and reclaim through Launchpad still need certification. The chart canary was deleted; the draft remains non-orderable.

The [candidate registry policy](/Users/jkershaw/Documents/launchpad/config/catalog-intake-candidate-policy.yaml) is scoped to this catalog ID, source revision, exact GHCR digest, Arena, internal exposure, and one seat. `.venv/bin/python scripts/check_catalog_intake_candidate.py` now returns `GREEN-local-candidate` against the draft and hash-pinned current pull receipt, while explicitly returning `release_eligible: false`. The [chart update](/Users/jkershaw/Documents/hybrid-fraud-detection/chart/templates/deployment.yaml) reads endpoint, model name, and key from a namespaced Secret. This local result is not a trusted render receipt, a demonstration that the selected model gateway enforces a per-seat key, catalog activation, or authorization to order a lab. The general platform and pilot registry policies remain unchanged.

## MaaS candidate authorization and inference gate — GREEN; full one-seat release gate remains RED

The [Arena model-discovery probe](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-model-auth-negative-20260922.json) and [bounded inference probe](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-inference-auth-negative-20260922.json) called the configured Granite endpoint from disposable, Launchpad-labeled namespaces. Both `GET /v1/models` and one-token `POST /v1/chat/completions` requests returned HTTP 200 without an Authorization header and with a deliberately invalid bearer token. The model Deployment has no API-key startup argument; the current route is a direct vLLM Service. Both temporary namespaces were removed and verified absent. Therefore injecting a generated seat key into `fraud-model-runtime` would **not** make that key an access control. Do not certify a seat or activate the draft on this path.

The separately scoped path now exists. `granite-3-2-8b-tools-candidate` reuses the five-ready-replica Granite deployment through Arena MaaS without creating another model workload or changing the direct model route. The candidate subscription and authorization policy are bound to one dedicated service identity. Model discovery returned only the candidate alias; a valid short-lived key produced a real HTTP 200 Granite response with the exact expected content, while missing auth returned 401 and an invalid key returned 403. A disposable key was revoked and denied, a separate one-minute key was denied after expiry, and one deterministic inference produced 23 model-reported tokens while the candidate Limitador counters advanced by one call and 23 hits. The installed gateway returned 403 for revoked and expired keys rather than the documented MaaS API 401; both security outcomes were denial. Arena's private ingress CA is trusted only for the exact candidate upstream host, with certificate verification still enabled. See [the MaaS candidate evidence](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/arena-maas-candidate-model-green-20260922.json).

This is not yet a full one-seat GREEN. Candidate-scoped key expiration, revocation, and basic usage accounting are proven, but Launchpad must still issue and revoke a key as part of seat lifecycle, the source-bound chart must run live against the MaaS Secret, and the participant/Showroom/RBAC/reclaim journey must finish with zero residue. Keep the item internal, one-seat only, draft, and `release_eligible: false` until those gates pass. The current Arena lifecycle worker was not restarted or changed.

Launchpad now has a disabled-by-default [RHOAI MaaS broker contract](/Users/jkershaw/Documents/launchpad/evidence/runs/catalog-intake-hybrid-fraud/launchpad-rhoai-maas-broker-green-local-20260922.json). It maps the existing seat-key lifecycle to subscription-bound MaaS issue and revoke endpoints, reads a rotating mounted control credential, requires TLS verification with an optional mounted CA bundle, recovers interrupted cleanup only when the persisted alias and key ID both match, and preserves secret-free reclaim receipts. Fifty-three local key-lifecycle, cleanup, and response-security tests pass. The broker itself also minted a disposable live candidate key, discovered only the candidate model, revoked by server key ID, and verified the stored status was `revoked`, without recording the plaintext key. This is not deployed: LiteLLM remains the default, no active control-plane configuration changed, and the candidate model endpoint, Launchpad service-account policy, live seat issuance, and reclaim proof remain open.

On September 22, the user broadened approval to shared MaaS prerequisites. The read-only prerequisite check found that Arena's default MaaS Tenant is `Failed`, but the more important discovery is that `default-dsc` explicitly sets `spec.components.kserve.modelsAsService.managementState: Removed`. The old Tenant's error still names `redhat-ods-applications` for its missing database Secret; **do not use that stale message as the 3.5 installation target**. OpenShift AI 3.5 [derives the infrastructure namespace from `MaasTenantConfig.status.infraNamespace`](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/govern_llm_access_with_models-as-a-service/deploy-and-manage-models-as-a-service), and that CRD does not currently exist here. Arena has OpenShift AI 3.5.1 and the Authorino Operator, but no Red Hat Connectivity Link subscription or Kuadrant CRD. OperatorHub currently offers only the community `kuadrant-operator` package, which is not a supported substitute for the Red Hat Connectivity Link prerequisite. The `maas-default-gateway` exists and its GatewayClass uses the OpenShift controller, but it lacks the required TLS bootstrap and ownership annotations. User Workload Monitoring is enabled. No shared services were installed or existing routes changed. Do not connect MaaS to a live Launchpad database or replace the direct vLLM path as a shortcut.

Bootstrap in stages: make the supported `rhcl-operator` package available in Arena's `redhat-operators` catalog (and verify subscription entitlement), then install a supported 1.4.1+ release in the namespace required by the OpenShift AI integration; create a ready Kuadrant instance and configure Authorino service TLS. Provision a dedicated PostgreSQL 14+ database with its own backup/retention, enable MaaS only after its prerequisites are present, resolve the generated `MaasTenantConfig.status.infraNamespace`, and create the `DB_CONNECTION_URL` Secret **there**, never in Git. Configure the Gateway annotations and verify Tenant/API health without altering existing vLLM routes. Then review a supported model publication path for the existing bare Granite Deployment before creating a narrowly scoped subscription and authorization policy. Verify valid/missing/invalid/revoked key behavior, request accounting, and direct-access isolation in a disposable one-seat namespace before touching candidate routing. The installed `MaaSModelRef` schema accepts `LLMInferenceService` or `ExternalModel`, not a bare Deployment. Run `.venv/bin/python scripts/preflight_hybrid_fraud_maas.py --kubeconfig /Users/jkershaw/Documents/launchpad/.arena-kubeconfig` to recheck prerequisites without reading Secret values or changing the cluster. The item remains draft and one-seat RED.
